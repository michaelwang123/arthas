// Package hub implements the Arthas Hub public room directory.
// It provides an in-memory registry of public rooms, an HTTP API for
// browsing and filtering the directory, per-IP rate limiting, and
// input validation for listing metadata.
package hub
