# Arthas — 在线网页多人游戏

## 项目概述

在线网页多人游戏，面向北美或欧洲用户，以最小资源成本部署运行。

---

## 技术栈

### 前端

- Vite + React + PixiJS + Zustand
- Tailwind CSS
- 资源格式：WebP（最大化 Vercel CDN 优势）

### 后端

- Go + Gorilla WebSocket
- PostgreSQL（Supabase / Neon 免费版）
- Redis（Upstash 免费版）

---

## 部署方案（最小资源）

| 层级 | 平台 | 说明 |
|------|------|------|
| 前端 | Vercel | 静态资源 + CDN |
| 后端 | Hugging Face Spaces | Go WebSocket 服务 |
| 保活 | Cron-job.org | 每 10 分钟 ping 后端，防止实例休眠 |

---

## 网络与同步架构

### Tick Rate

- 服务器 Tick Rate 设为 **20–30 Hz**（每秒 20–30 次逻辑更新）
- 降低 CPU 占用，使免费 HF Spaces 实例能承载更多同时在线玩家

### 心跳保活（Ping/Pong）

- HF Spaces 反向代理层对长连接较严格
- Go 后端每 **20–30 秒**向前端发送 Ping，前端自动回复 Pong
- 作用：防止中间代理层超时断连；实时监测玩家 RTT，可在 UI 显示延迟数值

### 自动重连

- 前端 WebSocket 实现自动重连机制，应对网络波动

### 协议优化

- 引入 **MessagePack**（二进制序列化），降低带宽消耗、提升解析速度

### 客户端预测（Client-side Prediction）

- 玩家按下方向键时，前端 PixiJS **立即**移动角色
- 同时向后端发送移动指令
- 若后端返回位置不符，进行平滑校准
- 效果：即使 100ms 延迟下，操作手感仍接近本地游戏

### 实体插值（Entity Interpolation）

- 解决"其他玩家"的移动平滑问题
- 前端维护坐标缓冲区（Buffer），在旧坐标与新坐标之间做线性插值（LERP）
- 效果：即使网络抖动，其他玩家也是平滑滑动而非瞬移

---

## 反作弊机制

> 一旦有排名或内购，就会有外挂。永远不要信任客户端传来的数据。

### 策略：服务器授权检查（只查核心逻辑）

- 服务器**不需要**每帧模拟（节省资源）
- 检查玩家的"物理极限"：
  - 1 秒内位移超过最大移动速度 → 判定异常
  - 攻击频率超过武器极限 → 判定异常
- 异常处理：直接断开该连接

---

## 目标用户

- 北美 / 欧洲地区玩家
