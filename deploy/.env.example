# --- Arthas Self-Hosted Configuration ---
# 📚 学习要点: 环境变量配置模板
# 此文件是 .env 的模板，deploy.sh 脚本会根据用户输入生成实际的 .env 文件。
# 复制此文件为 .env 并填写必要的值，或直接运行 deploy.sh 进行交互式配置。

# 域名（必填）
# 用于 Caddy 自动 HTTPS 证书申请和 WebSocket 来源验证。
# 公网部署: 填写你的域名，例如 chat.example.com
# 本地测试: localhost (使用 ./deploy.sh --local 自动设置)
DOMAIN=

# Let's Encrypt 邮箱（公网部署必填，localhost 可留空）
# Caddy 使用此邮箱向 Let's Encrypt 注册账户并接收证书到期提醒。
# 当 DOMAIN=localhost 时此项可留空（本地模式不使用 HTTPS）。
EMAIL=

# Docker 镜像版本（可选，默认 latest）
# 对应 ghcr.io 上发布的镜像 tag，例如 v1.0.0 或 latest。
# 建议生产环境使用具体版本号以确保可重复部署。
ARTHAS_VERSION=latest

# GitHub 用户名/组织名（镜像仓库前缀）
# 用于拼接完整的镜像地址: ghcr.io/{GITHUB_OWNER}/arthas:{ARTHAS_VERSION}
# 填写发布镜像的 GitHub 用户名或组织名。
GITHUB_OWNER=your-github-username

# WebSocket 允许的来源（通常由 deploy.sh 自动设置）
# 控制哪些网页来源可以建立 WebSocket 连接（CORS 安全策略）。
# 公网部署: https://{DOMAIN}（仅允许自己的域名）
# 本地测试: * (允许所有来源，方便开发调试)
# 一般无需手动设置 — deploy.sh 会根据 DOMAIN 自动生成正确的值。
# ALLOWED_ORIGINS=
